services:
  frontend:
    image: registry.simpletactics.de/sitafrontend:2026-01-26.146
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        order: start-first
      restart_policy:
        condition: on-failure
    networks:
      - main

  backend:
    image: registry.simpletactics.de/sitabackend:2.5.1
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        order: start-first
      restart_policy:
        condition: on-failure
    environment:
      - TZ=Europe/Berlin
    networks:
      - main
    secrets:
      - DB_USERNAME
      - DB_PASSWORD
      - SITA_BOT_ALERTING_URL
      - source: DB_CONNECTION_STRING_DEV
        target: DB_CONNECTION_STRING
        mode: 0400
      - source: SITA_BOT_ALERTING_CHANNEL_ID_DEV
        target: SITA_BOT_ALERTING_CHANNEL_ID
        mode: 0400

  socket:
    image: registry.simpletactics.de/sitasocket:1.0.0
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        order: start-first
      restart_policy:
        condition: on-failure
    networks:
      - main

networks:
  main:
    external: true

secrets:
  DB_USERNAME:
    external: true
  DB_PASSWORD:
    external: true
  DB_CONNECTION_STRING_DEV:
    external: true
  SITA_BOT_ALERTING_URL:
    external: true
  SITA_BOT_ALERTING_CHANNEL_ID_DEV:
    external: true
